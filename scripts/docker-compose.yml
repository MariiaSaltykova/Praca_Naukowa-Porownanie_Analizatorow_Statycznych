services:
  mysql-local:
    image: mysql:8
    container_name: mysql-local
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_DATABASE: metryki
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
